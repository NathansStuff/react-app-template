import { IPaidFrequency } from './PaidFrequency';

export const mockPaidFrequencyProps: IPaidFrequency = {
  baseId: 'display-basic',
  showErrors: false,
};

export const mockPaidFrequencyErrorProps: IPaidFrequency = {
  baseId: 'display-basic',
  showErrors: true,
};
