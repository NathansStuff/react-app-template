import { IGenderProps } from './Gender';

export const mockGenderProps: IGenderProps = {
  baseId: 'display-basic',
  showErrors: false
};

export const mockGenderErrorProps: IGenderProps = {
  baseId: 'display-basic',
  showErrors: true,
};
