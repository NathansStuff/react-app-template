import { ILivingStatusProps } from './LivingStatus';

export const mockLivingStatusProps: ILivingStatusProps = {
  baseId: 'display-basic',
  showErrors: false,
};

export const mockLivingStatusErrorProps: ILivingStatusProps = {
  baseId: 'display-basic',
  showErrors: true,
};
