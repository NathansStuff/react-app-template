import { ICasualLength } from './CasualLength';

export const mockCasualLengthProps: ICasualLength = {
  baseId: 'display-basic',
  showErrors: false,
};

export const mockCasualLengthErrorProps: ICasualLength = {
  baseId: 'display-basic',
  showErrors: true,
};
